import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: 'rgb(255, 255, 255)'
        },
        dark: {
            main: 'rgb(236,102,82)'
        }
    }
})

export default theme;
